package Abdelhafid.pglp_4_2;

import java.util.HashMap;
import java.util.Stack;

/** The Invoker class */

public class Interpreteur {
	
	/*
	private final HashMap<String, GeneriqueCommand> commandMap = new HashMap<String, GeneriqueCommand>();
	
	

	
	
	
	
	 public void enregister_cmd(String commandName,  GeneriqueCommand command) {
	        commandMap.put(commandName, command);
	    }
	    
	    public void executer_cmd(String commandName) {
	    	
	    	 GeneriqueCommand command = commandMap.get(commandName);
	        if (command == null) {
	            throw new IllegalStateException("no command registered for " + commandName);
	        }
	        
	        
	        command.apply();
	        
	        this.historique.add(command);
	    }

	    */
	    

}

